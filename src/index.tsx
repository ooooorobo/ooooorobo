import React from "react"
import ReactDOM from "react-dom"
import App from "@src/pages/App"
import "@src/styles/pages/index.modules.scss"

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById("root"),
)
